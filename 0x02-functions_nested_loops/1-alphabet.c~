#include <stdio.h>
/**
 *Write a function that prints the alphabet, 
 *in lowercase, followed by a new line.
 */
int main(void)
{
  int i;
  for (i ='a';i <'z'; i++)
    {
      putchar(i);
    }
  putchar('\n');
}
